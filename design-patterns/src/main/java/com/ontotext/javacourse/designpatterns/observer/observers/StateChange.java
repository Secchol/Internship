package com.ontotext.javacourse.designpatterns.observer.observers;

/** The StateChange enumeration indicates whether a shoe is sold or a shoe is added. */
public enum StateChange {
  SHOE_SOLD,
  SHOE_ADDED
}
