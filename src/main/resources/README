Create executable JAR from Hangman task.

Using cmd:
jar cfe Hangman.jar Runner.class * - creates executable JAR from Hangman task.
java-jar Hangman.jar - executes jar file.
Using maven:
mvn install-creates JAR file and stores the classes in the local repository.
Using IntellJ:
File-Project Structure-Artifacts-Add JAR-From modules with dependencies-introducing-java-with Hangman as runner class

Package introduction-java as JAR:
mvn package(install)